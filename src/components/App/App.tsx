import React, { FC } from 'react'

import ModalManager from '../Modal/Modal'

const App: FC = () => {
  return (
    <div className="relative">
      <ModalManager />
    </div>
  )
}

export default App
